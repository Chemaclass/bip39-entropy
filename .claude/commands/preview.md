---
description: Rebuild index.html and open it in the browser for a print-layout check
allowed-tools: Bash(python3 scripts/build.py:*), Bash(open index.html), Read
---

Rebuild with `python3 scripts/build.py`, then `open index.html`.

Tell the user what to look at, based on what changed in this session: the control
bar, the type-size readout, search highlighting, and ⌘P print preview with
background graphics on at 100 % scale. Note the reference type sizes for the papers
the change could affect (see the `poster` skill).
